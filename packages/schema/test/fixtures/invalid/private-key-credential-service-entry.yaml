# Private-key family: key/credential. Nested inside a services[] entry
# (not the root) — the schema guardrail has to reach this deep, not just
# guard the top level. camelCase variant (apiKey) exercises case handling.
dagstree: 1
project:
  name: Clapline
  slug: clapline
services:
  - id: anthropic-api
    service: anthropic
    role: ai-models
    added: 2026-06-01
    apiKey: "sk-ant-example"
dependencies: []
