# The edge names "vertex", but no services[] entry has that id — also
# referential integrity, not expressible in JSON Schema.
catalogus: 1
project:
  name: Clapline
  slug: clapline
services:
  - id: fly
    service: fly-io
    role: hosting
    added: 2025-11-02
dependencies:
  - [fly, vertex]
