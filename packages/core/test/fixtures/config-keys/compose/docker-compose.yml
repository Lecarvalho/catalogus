services:
  postgres:
    image: postgres:16
  redis:
    image: redis:7
  loki:
    image: grafana/loki:3.0.0
  api:
    build: .
    environment:
      - SENTRY_DSN=
      - RESEND_API_KEY=
    depends_on:
      - postgres
